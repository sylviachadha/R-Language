# Dataframe Recap
#-------------------

# Import data using csv files or manually choose file
# Explore dataset using head, tail, summary, str
# Using $ sign to access columns of dataframe by name
# Basic operations with df add, remove columns by setting to NULL
# Factors have different levels this is how R stores categorical variables
# Filtering df - selecting rowa u want based on some certain criteria
# qplot to visualize using a scatterplot 
# Building / creating dataframe using data.frame function similar to cbind
# used in matrix
# Merging df using merge function that is how to join 2 df together 
# based on 1 column that they share. Also like inner join and outer join
# qplot features like changing size, colour, shape, transparency (alpha parameter),
# changing title






